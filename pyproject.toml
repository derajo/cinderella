[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cinderella"
version = "0.1.0"
dependencies = ["pandas", "numpy"]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
